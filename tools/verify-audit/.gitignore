# Reports ARE committed — they are the evidence Tier 1 is read from, and an
# audit that leaves nothing behind is the defect this directory exists to fix.
# Only the gate's stderr scratch is ignored.
*.gate
