pack: the third copy said the paper had no deposit. It does.

This is the standalone copy at the geometry root -- the third of three, and the
one that never got the 2026-08-01 fixes the two AXLE copies received. It had
drifted further than "stale": someone had already corrected the phantom series
DOI here, but corrected it to the wrong conclusion.

Both reference entries read "No standalone Zenodo record found under this
title." That is false. The record exists and has three DOIs: v1
10.5281/zenodo.21013066 (June 2026), v2 10.5281/zenodo.21753025 (August 2026),
and concept 10.5281/zenodo.21013065, which resolves to whichever is current.
Checked against the Zenodo API 2026-08-02.

The reason it looked absent is the same defect in a different guise. Searching
on 19117399 lands on Principia Orthogona Vol. I -- today Vol. I v6,
10.5281/zenodo.21146416 -- so the searcher concluded the network-games paper was
never deposited rather than that the DOI belonged to another work. A phantom
citation produced a phantom absence.

Entry (2026b) had the same false negative plus a second error: it said the
companion was "not yet deposited, or not yet written." It is neither. It is the
ADDENDUM inside the parent record, so it has the parent's DOI. The one part of
that entry that was right, and is kept, is that the circadian-trader row remains
unsourced.

Also brought in line with the AXLE copies, all previously fixed there:

  - Network diagram and value-gap chart were blanked one frame after painting.
    ResizeObserver fires its initial callback after the inline draw(); the
    callback assigns cv.width, which clears the canvas, and nothing repainted.
    The orbit canvas is left alone -- its rAF loop masks the same bug, and
    adding draw() to its observer would start a second loop per resize.
  - Scoreboard had six cells under auto-fit, which resolved to five columns near
    1000px and left the sixth beside four empty tracks, painting the grid's
    border colour as a solid block. Now a 6/3/2 ladder.
  - Nav was overflow-x:auto with no scroll affordance, silently clipping Open
    Problems / Sorry Audit / References. Now wraps.
  - Hero cited 19117399 directly under "Papers: Grossi (2026a) - Grossi (2026b)",
    where it reads as the paper's DOI. Now v2 plus the concept DOI. Footer
    pointed at the same number as "Zenodo series"; now the community URL.

One occurrence of 19117399 remains, inside the correction notice, quoting the
claim being retired.

Three copies of this pack now agree. They should not have been three.
