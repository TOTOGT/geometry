# Handoff · 18 September 2026

Ten pages written today, nine producing scripts, Volume XI's `sorry` discharged.
Everything below is open. Nothing here is urgent.

---

## 0 · Do first (one command)

- [ ] **`git push`** — three commits outstanding:
      `6982a76` index entries · `4e5d36b` attribution removed · `2ba1377` WP-41 corrections
- [ ] **Clear the git locks** from a real terminal. The sandbox cannot unlink in
      the connected folder, so every git call leaves `.git/index.lock` behind and
      I work around it by renaming. Harmless, accumulating:
      ```
      rm -f .git/*.lock .git/stale-*
      find .git/objects -name 'tmp_obj_*' -delete
      ```

---

## 1 · Gating — these decide whether something else is worth doing

- [ ] **WP-127 gap Q7 · SAF certification.** No ILUC or sustainability analysis
      anywhere in the paper. Whether any pathway certifies under **CORSIA,
      ReFuelEU, RenovaBio** determines whether the commercial thread has a market
      at all. Date palm *residue* is a waste stream and should sit in a good
      category — but "should" is not a certification.
- [ ] **WP-127 gap Q5 · the yield band.** 12–22 % mass yield to SAF is plausible
      for lignocellulosic routes generally and is sourced to **no date palm
      study**. The TBFCo brief's UAE citation establishes *feasibility*, which is
      not a yield. A counterparty's technical reviewer finds this quickly.
- [ ] **Book X ch06 gap P3 · Moyo versus coordination.** Two incompatible accounts
      of the same fact, pointing opposite ways on what to do. The only genuinely
      *unfinished argument* in today's work rather than a declared limit. The
      evidence that separates them exists; reading it is the task.

---

## 2 · Open in the corpus

- [ ] **WP-41 · both ends of the relocation range.** Corrected today to
      "near zero if people move themselves and are allowed to arrive;
      $100–200k if a destination is built." Neither end re-derived. `[OPEN]`
- [ ] **WP-41 · the unit problem.** The figure is denominated in a currency whose
      meaning is conditional on the scenario not arriving. Recorded, not resolved.
- [ ] **Book VIII gap K4 · the deuterium limit.** ~13 M_Jup, the planet/brown-dwarf
      boundary, same machinery as ch8-8b at T_ign ≈ 5×10⁵ K. **About an hour.**
      Left out because the 38 % systematic wasn't pinned well enough to print a
      second uncalibrated prediction.
- [ ] **Book VII gap G2 · a closed form for 1103.** 8·1103 sits inside α(58),
      verified to 250 places. Running the derivation the *other* way — from the
      alpha function to the coefficient — makes 1103 as derived as 9801 and 26390.
- [ ] **Book VII gap H1 · "the last of six".** The sweep covers N ≤ 600. The reason
      to expect no larger N is class-field theory, cited not shown. Until then the
      title is a claim with a bound on it.
- [ ] **Book VII gap L1 · Chandrasekhar stability.** §4 finds where the Jacobi
      branch *meets* Maclaurin. It does not compute which branch is stable, and
      does not separate viscous from dissipationless — which give *different*
      critical points on this very sequence.
- [ ] **Book VII gap N2 · TSP at scale.** No large-instance heuristic gap is
      claimed; the 11–13 % against the 1-tree bound at n = 1000 conflates
      heuristic error with bound looseness, and the bound's quality there is
      unestablished.
- [ ] **Vol XI · what is still missing upstream.** The projective-module monoid.
      That is a gap in Mathlib, not in `VolXI_K0_Floor.lean`, which now compiles
      clean with no `sorryAx`.

---

## 3 · Carried over from 17 September

- [ ] **WP-124 collision.** Two chapters carry that number. Owner's call; no
      script will rename a published file.
- [ ] **`ch-strogatz.html` and Chapter R §VI** — the readability pass.
- [ ] **k₂₁₀** still needs the printed page.
- [ ] **`certify_rstar.py`** DOI fix across 7 copies.
- [ ] **60 scripts record no limits** (of 122; 62 do, only 5 machine-readably).
- [ ] **`sluing/neuro` push** — 5 commits ahead, needs credentials. Repo is public
      and the Spine page names a clinic.

---

## 4 · Not corpus work, noted so it isn't lost

- Brazil sales tour, solved and proven optimal:
  **São Paulo → Curitiba → Brasília → Belo Horizonte → Rio → São Paulo**, 2 743 km.
- Field coverage: **SP + Rio + Campinas is 54 % of weighted demand**; the six
  southeast/centre metros are 75 %. The districting lever is larger than the
  routing lever and only the routing one is famous.
- The commodity platform: the cold-start problem *is* WP-126. A two-sided
  marketplace is a coordination game with a stable low-participation equilibrium,
  and the model puts the tipping fraction at 4–17 %.
- Selling contracts connecting buyers and sellers in Brazil has **CVM**
  implications. Brazilian counsel early, not late.
