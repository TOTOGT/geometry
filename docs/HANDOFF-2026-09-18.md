# Handoff · 18 September 2026

Ten pages written today, nine producing scripts, Volume XI's `sorry` discharged.
Everything below is open. Nothing here is urgent.

---

## 0 · Do first (one command)

- [ ] **`git push`** — three commits outstanding:
      `6982a76` index entries · `4e5d36b` attribution removed · `2ba1377` WP-41 corrections
- [ ] **Clear the git locks** from a real terminal. The sandbox cannot unlink in
      the connected folder, so every git call leaves `.git/index.lock` behind and
      I work around it by renaming. Harmless, accumulating:
      ```
      rm -f .git/*.lock .git/stale-*
      find .git/objects -name 'tmp_obj_*' -delete
      ```

---

## 1 · Gating — these decide whether something else is worth doing

- [ ] **WP-127 gap Q7 · SAF certification.** No ILUC or sustainability analysis
      anywhere in the paper. Whether any pathway certifies under **CORSIA,
      ReFuelEU, RenovaBio** determines whether the commercial thread has a market
      at all. Date palm *residue* is a waste stream and should sit in a good
      category — but "should" is not a certification.
- [ ] **WP-127 gap Q5 · the yield band.** 12–22 % mass yield to SAF is plausible
      for lignocellulosic routes generally and is sourced to **no date palm
      study**. The TBFCo brief's UAE citation establishes *feasibility*, which is
      not a yield. A counterparty's technical reviewer finds this quickly.
- [ ] **Book X ch06 gap P3 · Moyo versus coordination.** Two incompatible accounts
      of the same fact, pointing opposite ways on what to do. The only genuinely
      *unfinished argument* in today's work rather than a declared limit. The
      evidence that separates them exists; reading it is the task.

---

## 2 · Open in the corpus

- [ ] **WP-41 · both ends of the relocation range.** Corrected today to
      "near zero if people move themselves and are allowed to arrive;
      $100–200k if a destination is built." Neither end re-derived. `[OPEN]`
- [ ] **WP-41 · the unit problem.** The figure is denominated in a currency whose
      meaning is conditional on the scenario not arriving. Recorded, not resolved.
- [ ] **Book VIII gap K4 · the deuterium limit.** ~13 M_Jup, the planet/brown-dwarf
      boundary, same machinery as ch8-8b at T_ign ≈ 5×10⁵ K. **About an hour.**
      Left out because the 38 % systematic wasn't pinned well enough to print a
      second uncalibrated prediction.
- [ ] **Book VII gap G2 · a closed form for 1103.** 8·1103 sits inside α(58),
      verified to 250 places. Running the derivation the *other* way — from the
      alpha function to the coefficient — makes 1103 as derived as 9801 and 26390.
- [ ] **Book VII gap H1 · "the last of six".** The sweep covers N ≤ 600. The reason
      to expect no larger N is class-field theory, cited not shown. Until then the
      title is a claim with a bound on it.
- [ ] **Book VII gap L1 · Chandrasekhar stability.** §4 finds where the Jacobi
      branch *meets* Maclaurin. It does not compute which branch is stable, and
      does not separate viscous from dissipationless — which give *different*
      critical points on this very sequence.
- [ ] **Book VII gap N2 · TSP at scale.** No large-instance heuristic gap is
      claimed; the 11–13 % against the 1-tree bound at n = 1000 conflates
      heuristic error with bound looseness, and the bound's quality there is
      unestablished.
- [ ] **Vol XI · what is still missing upstream.** The projective-module monoid.
      That is a gap in Mathlib, not in `VolXI_K0_Floor.lean`, which now compiles
      clean with no `sorryAx`.

---

## 3 · Carried over from 17 September

- [ ] **WP-124 collision.** Two chapters carry that number. Owner's call; no
      script will rename a published file.
- [ ] **`ch-strogatz.html` and Chapter R §VI** — the readability pass.
- [ ] **k₂₁₀** still needs the printed page.
- [ ] **`certify_rstar.py`** DOI fix across 7 copies.
- [ ] **60 scripts record no limits** (of 122; 62 do, only 5 machine-readably).
- [ ] **`sluing/neuro` push** — 5 commits ahead, needs credentials. Repo is public
      and the Spine page names a clinic.

---

## 4 · Not corpus work, noted so it isn't lost

- Brazil sales tour, solved and proven optimal:
  **São Paulo → Curitiba → Brasília → Belo Horizonte → Rio → São Paulo**, 2 743 km.
- Field coverage: **SP + Rio + Campinas is 54 % of weighted demand**; the six
  southeast/centre metros are 75 %. The districting lever is larger than the
  routing lever and only the routing one is famous.
- The commodity platform: the cold-start problem *is* WP-126. A two-sided
  marketplace is a coordination game with a stable low-participation equilibrium,
  and the model puts the tipping fraction at 4–17 %.
- Selling contracts connecting buyers and sellers in Brazil has **CVM**
  implications. Brazilian counsel early, not late.

---

## 5 · Other repositories

**`Desktop/geometry` is the one that must be perfect.** Everything below is
somewhere else and is listed so it stops being carried in anyone's head.
State read off each working tree on 18 September; "not inspected" means
exactly that, not "fine".

### Has unpushed work

| repo | remote | unpushed | dirty |
|---|---|---|---|
| **vol1-proofs** | `TOTOGT/vol1-proofs` | **2** | 1 |
| **neuro** | `sluing/neuro` | **5** | 2 |
| **AXLE** | `TOTOGT/AXLE` | **1** | 6 |

- [ ] **vol1-proofs — push, then watch CI.** Fixed 18 Sep: build restored at
      the v4.14.0 pin (the import had been moved to the v4.32 path), counts
      reconciled to **82** (58 `PrincipiaVol1` + 24 `AutophagyDm3`; 81 on the
      three standard axioms, one on none, zero `sorryAx`), README opened with
      a badge and the reproduce command, v4.32 forward debt documented in
      `docs/MATHLIB-FORWARD-v4.32.md`. **Not rebuilt locally — no Lean
      toolchain on this machine. CI is the proof.** Do not link this in the
      Axiom application until the badge is green.
      `tools/vacuity_fixtures.lean` is deliberately uncommitted; leave it.
- [ ] **AXLE — apply the same import patch.** Same bad import as vol1-proofs
      had. Do **not** put AXLE in a job application: 69 MB, most Lean never
      built, `Monotonicity.lean` and `TribonacciMeasure.lean` fail to compile,
      `AXLE.lean` imports a file that does not exist.
- [ ] **neuro — push needs credentials.** 5 ahead. Repo is public and the
      Spine page names a clinic; read it once before it goes up.

### The R\* thread — DONE, and it turned up something bigger

I wrote "NOT STARTED" here and it was wrong. Two commits in **this**
repository, unpushed, are the audit:

- `6b54adf` — `grossi-ops.github.io/TO` had served the withdrawn r\* for 77 days
- `21a4213` — the audit: *the equations were the typo*, with
  `tools/to_hub_verify.py`, five blocks, exit 0

Checked live 18 Sep 23:xx: **TO now serves `0.77594058`, not 0.773.** The
deploy appears to have happened between the audit and now.
(`/TO/hub.html` is 404; the page is at `/TO/`.)

- [ ] **r\* disagrees with itself, 197 times.** `[SHOWN]` — arithmetic, not a
      re-derivation.

      | form in the corpus | occurrences |
      |---|---|
      | `0.77594058` | **197** |
      | `0.77594` | 21 |
      | `0.77594058` | 14 |
      | four distinct high-precision forms | 9 + 7 + 2 + 2 |

      The four high-precision forms agree to **ten decimals** — `0.7759405755`
      — and diverge after. **All four round to `0.77594058` at 8 dp.**
      `0.77594058` is the correct 8-dp rounding of none of them, and commit
      `70d08d7` calls it *"the certified 0.77594058"*.

      So the corpus's most-repeated value is an off-by-one in the last digit,
      carried 197 times, and the live page and today's audit script both use
      the *correct* one — which is how the two came to disagree.

      What is **not** established: which high-precision form is right. They
      part company at the 11th decimal and r\* was not re-derived from its
      defining equation here. That is the next step, and it must come before
      any mass edit — replacing 197 instances with a value that is itself
      unverified would make it worse.

- [ ] **`certify_rstar.py` — rescope or drop.** Carried from 17 Sep as a
      "DOI fix across 7 copies". `21a4213` records that v1.1's path does not
      exist and no copy on this desk matches it. Establish what that script
      now is before touching it.

- [ ] **The class of error, recorded in `docs/audit-log.md` by `6b54adf`:**
      a correction whose scope was *asserted* rather than checked. "Fix dm3
      math: r\*=0.77594058 across series" was a claim about other
      repositories, and nothing read them. The 197-count above is the same
      shape a second time.

### Clean but dirty working trees — not inspected

`GTCT` (7 files), `dnls` (8), `b3s` / book3-starter (6), `Atratores` (1),
`g6` → `G6LLC/g6llc.github.io` (1). All at origin; none looked at. The
uncommitted changes may be someone's work in progress or may be noise.

### Clean, at origin, not inspected

`3M`, `cajueiro`, `grossi-ops/g6`, `io-clone`, `probes-clean`,
`NavierStokesAndEuler` (a fork, not yours).

### The standing problem

Fifteen local repositories, four with unpushed work. That is the mechanism
behind "I sometimes trust things have been done and they haven't." A weekly
`git status` sweep across all of them would surface it in one command; the
loop that produced this table is in this session's history and could be a
script.

---

## 6 · The sorryAx sweep, 18 Sep — geometry is clean

Prompted by a fair criticism: r\* was a rounding digit and I was gating on it
while `21a4213` had already named worse. Went and looked.

**r\* is done.** 224 occurrences in 78 files set to `0.77594058` (`f1db753`),
which is the correct 8-dp rounding of every high-precision form here. The
thirteen high-precision forms are deliberately untouched; they diverge at the
11th decimal and r\* is still not re-derived. `[OPEN]`

**22 theorems in this corpus depend on `sorryAx`.** Named in
`tools/verify-audit/2026-09-09/` — `AXLE_v8_1` (12), `counting` (3),
`Ordinal` (3), `ZetaReflection` (2), `MagneticLattice` (1), `SeismicLattice`
(1) — including `TOGT.collatz_conjecture_via_dm3_gqm`,
`TOGT.g6_unconditional_closure` and `Book4.Ch12.reflection_law`.

**Checked whether the published pages disclose it. They do.** Every page
naming one of those theorems also carries the disclosure; `book4/ch12.html`
is the thinnest by word count and reads:

> *Admitted: Result 12.1 alone, as `reflection_law`, which carries `sorryAx`
> … Numerically confirmed is not proved, and the file says which is which.*

So the discipline is holding. `[SHOWN]` — no undisclosed `sorryAx` claim was
found in geometry.

### The three serious findings in `21a4213` are NOT in this repository

- [ ] **"Three axioms, not two, and the third was never named."** Not
      reproducible here — geometry's reports name their axioms.
- [ ] **Two divergent `Chain_updated.lean`, one sorry-free and one not.**
      `Chain_updated.lean` does not exist in geometry. It belongs to `io` or
      `AXLE`. This is the same shape that produced vol1-proofs' V6 drift and
      is worth fixing wherever it lives.
- [ ] **`spiral_return_exists` assumes its conclusion** — takes *"the
      128-orbit does not return"* as a hypothesis and closes with
      `exact h_second_circuit`, so it proves the implication and not the
      antecedent. **A theorem that assumes its conclusion still
      kernel-checks**, so no axiom gate catches it. geometry carries
      `SpiralReturnObstruction.lean`, which *documents* the obstruction; the
      offending theorem is elsewhere.

**The general point, which outlives all three:** the axiom gate cannot see a
theorem that assumes what it claims. Vacuity scanning catches one form of
this; a hypothesis that is the conclusion is another, and nothing in this
corpus's harness looks for it. That is a real gap in the instrument, not in
any one file. `[OPEN]`
